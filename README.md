# Setup

The easiest way is to reopen this repository in devcontainer. This is done by clicken the popup "Reopen in container" or the icon on the lower left. At the first start, this will take a bit longer.

# Build the slides

Simply build the slides with this command:

    pandoc -t beamer slides.md -o slides.pdf